import { Screen } from '../components/Screen'

/** Placeholder: the paste-and-preview flow is built on Day 9. */
export function NewPlanScreen() {
  return (
    <Screen title="Add a plan">
      <section className="card today-placeholder">
        <p className="t-subhead secondary">The paste-and-preview flow arrives on Day 9.</p>
      </section>
    </Screen>
  )
}
